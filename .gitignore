# local dev overrides
docker-compose.override.yml

# dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/

# build
dist/
build/

# env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Prisma — migrations trackées depuis v4-LOY-3M (passage de db push à prisma migrate)

# IDE
.vscode/
.idea/

# MCP config (contains credentials — use .mcp.json.example as template)
.mcp.json

# Claude Code local config (machine-specific, not shared)
.claude/settings.local.json

# Raw data téléchargée (hors repo — trop lourde, répertoire reconstruit par probe/ingest)
data/cerema/
data/cartes-loyers/
data/dgfip-rei/

# Couverture de tests
coverage/
